apiVersion: v2
name: monitoring
description: Umbrella chart for EKS monitoring stack (Prometheus + Grafana)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: "67.x"
    repository: https://prometheus-community.github.io/helm-charts
